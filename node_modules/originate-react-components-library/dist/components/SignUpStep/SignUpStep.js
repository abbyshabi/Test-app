import React from 'react';
import PropTypes from 'prop-types';
import { Step } from 'react-albus/lib';

var SignUpStep = function SignUpStep(props) {
  return React.createElement(Step, null, React.createElement("div", {
    className: "sign-up-step"
  }, !props.title ? '' : React.createElement("h4", {
    className: "sign-up-step__title ".concat(props.smallTitle ? 'small-title' : '')
  }, props.title), React.createElement("div", {
    className: 'sign-up-step__content'
  }, props.children), props.note ? React.createElement("aside", {
    className: "note"
  }, "*NOTE: ", props.note) : ''));
};

SignUpStep.propTypes = {
  note: PropTypes.string,
  title: PropTypes.string,
  classes: PropTypes.string,
  smallTitle: PropTypes.oneOfType([PropTypes.bool, PropTypes.string]),
  id: PropTypes.string.isRequired
};
export { SignUpStep };